spring:
  application:
    name: WalmartApi
  datasource:
    url: jdbc:mysql://localhost:3306/walmart-db
    username: walmapi
    password: walmapi
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
